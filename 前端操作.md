# 前端展示框架

> **统一规则**：时间一律按照 `[yyyy-MM-dd hh:mm:ss]` 格式显示。
## Todo
* [ ] 处理网络延迟
* [ ] 前端防抖节流

## 1. 左侧边栏：永续合约币种清单
*注：侧边栏支持固定*

### 1.1 显示内容
* 最新价格
* 涨跌幅度
* 资金费率
* 成交量

### 1.2 操作与交互
* **双击**：可以向屏幕中添加当前币种 K 线信息。
* **侧边栏状态**：可固定。
* **排序维度**：
  * 最新价格
  * 涨跌幅度
  * 资金费率
  * 成交量
* **搜索**：支持根据名称搜索币种。

---

## 2. 右侧边栏：辅助交易功能清单
*注：侧边栏支持固定*

### 2.1 显示内容
* 辅助名称

### 2.2 功能与输入
* **操作**：支持开启/关闭功能。
* **参数输入**：辅助功能可能存在输入参数配置。

---

## 3. 主要部分

### 3.1 模块化基础功能
* **布局**：模块可自由拖动放置。
* **模块同步**：
  * 开关按钮位于模块顶部。
  * **联动绘制**：当在一个 K 线模块绘制水平线时，其他同币种的 K 线模块将同步绘制。

### 3.2 K 线显示模块
* **数据展示**：显示当前实时 K 线信息。
* **操作交互**：
  * 修改 K 线类型（支持：`K线`、`平均K线`、`折线`）。
  * 调节模块宽高大小。
* **🚀 性能优化**：当存在多个相同名称的 K 线组件时，复用同一个数据流，避免每个组件单独连接一个 WebSocket 通道。

### 3.3 仓位信息列表栏模块
* **显示内容**：支持多个 Tab 切换按钮。

### 3.4 待办事项 (Todo List) 模块
* **页面展示优化**:
  * [ ] 增加网络状态显示
* **性能优化**:
  * [ ] 当多个页面时候使用 `广播` 之类的方式进行优化 避免多链接问题

## 4. 下单模块

### 4.1 基础账户信息
* **余额**：显示当前可用余额。
* **保证金比率**：显示当前账户风险状态。

### 4.2 交易参数设置
* **仓位模式**：支持切换 `全仓` 或 `逐仓` 模式。
* **杠杆调节**：支持手动或滑动调节杠杆倍数。
* **价格设置 (下单模式)**：
  * **市价**：按当前市场最优价格快速成交。
  * **挂单价 (限价单)**：
    * 支持快捷勾选“获取当前最新价格”填入。
    * 支持手动输入指定目标价格。

### 4.3 投入金额配置
* **快捷固定金额**：提供快捷按钮（如 `10u`, `20u` 等）直接填入。
* **自定义调节金额**：
  * **滑块拖动操作**：支持按账户可用余额百分比拖动，提供快捷吸附节点（`25%`, `50%`, `100%`）。
  * **动态数值显示**：随着滑块拖动或输入，实时显示：
    * 实际投入价值 (USDT)
    * 乘以杠杆后的名义总价值 (USDT)

### 4.4 风险预估与执行
* **风险提示**：根据当前设置的参数，动态显示**预计爆仓金额/价格**。
* **执行动作**：
  * **开空 (卖出)** 按钮
  * **开多 (买入)** 按钮